

composer serve
ab -n 1000 -c 100 http://127.0.0.1:8888/
ab -n 1000 -c 100 http://gapi.com/



# 命令 command 系统


# 自动生成路由
php build auto route

# 生成单个版本路由
php build route vX.X.X

#  "symfony/mime": "^5.2.1"

# 解决方案，切换 composer 镜像地址
# composer config -g repo.packagist composer https://mirrors.aliyun.com/composer/